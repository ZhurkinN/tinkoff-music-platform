spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/music-platform
    username: postgres
    password: Iissupe5
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  security:
    user:
      name: user
      password: 333
  mvc:
    hidden-method:
      filter:
        enabled: true