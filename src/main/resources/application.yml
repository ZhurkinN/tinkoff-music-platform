# Spring
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/music-platform
    username: postgres
    password: 2108cret
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  security:
    user:
      name: user
      password: 333

# Minio
minio:
  bucket:
    name:
      minio-tinkoff-music-platform
  access:
    key:
      minioadmin
    secret:
      minioadmin
  url:
    https://play.min.io