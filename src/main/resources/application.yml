spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/musicClub
    username: postgres
    password: 2108cret
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  security:
    user:
      name: user
      password: 333